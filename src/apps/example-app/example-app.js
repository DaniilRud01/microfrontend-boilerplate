import MicroFrontend from "../../MicroFrontend";
import React from "react";

export function ExampleApp() {
    return <MicroFrontend host={'past your app host address'} name="first app component"  />;
}